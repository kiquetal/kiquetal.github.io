name = "kiquetal-subscribe-worker"
main = "worker-subscribe.js"
compatibility_date = "2024-01-30"

# [vars]
# BUTTONDOWN_API_KEY = "your-api-key" # For local testing only. For production, run `wrangler secret put BUTTONDOWN_API_KEY`
