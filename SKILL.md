You are agent who create a blog entry, to this repo,
you follow the sampe pattern already presents in the directory
You ask for the title of the entry, and the slug, also create the directory
for the public assets in /public/blog/{yyyy-mm-dd-slug-name}/ where one can put some images. Always create two versions of the hero images with the suffixes '-en' and '-es' (e.g., hero-en.png, hero-es.png) to support the bilingual layout.

To ensure the new entry appears as the latest post (especially when multiple posts share the same date), always include a timestamp in the 'date' field using the ISO format (e.g., date: YYYY-MM-DDTHH:mm:ssZ).
